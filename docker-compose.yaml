services:
  laserweb:
    image: mkuf/laserweb:latest
    container_name: laserweb
    restart: unless-stopped
    privileged: true
    environment:
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - ./config:/config
      - ./gcode:/gcode
      - /dev:/dev
    ports:
      - "80:80"
    logging:
      driver: json-file
      options:
        max-size: "2m"
        max-file: "1"
    networks:
      - web

  lw-comm-server:
    build: ./lw.comm-server
    container_name: lw-comm-server
    privileged: true
    restart: unless-stopped
    ports:
      - "8081:8000"
    volumes:
      - /dev:/dev
    environment:
      - TZ=Asia/Ho_Chi_Minh
    logging:
      driver: json-file
      options:
        max-size: "2m"
        max-file: "1"
    networks:
      - web

  # mjpg-streamer:
  #    image: registry.gitlab.com/luckyturtledev/docker-mjpg-streamer:latest
  #    ports:
  #         - '8082:8080'
  #    command: -i "input_uvc.so -n -d /dev/video0 -r 640x480 -f 15" -o "output_http.so -w  /usr/share/mjpg-streamer/www/"
  #    devices:
  #         - /dev/video0
  #    restart: unless-stopped
  #   networks:
  #     - web

  # lcd-display:
  #   build: ./IPLaserCNC
  #   container_name: lcd-hostname-ip
  #   restart: unless-stopped
  #   privileged: true
  #   devices:
  #     - "/dev/i2c-1:/dev/i2c-1"
  #   network_mode: "host"
  #   environment:
  #     - CONFIG_FILE=/app/config_board.yaml
  #   mem_limit: 50m
  #   logging:
  #     driver: json-file
  #     options:
  #       max-size: "2m"
  #       max-file: "1"
  #   tty: true
  #   stdin_open: true

networks:
  web:
    driver: bridge
